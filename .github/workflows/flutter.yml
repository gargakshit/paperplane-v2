name: Run flutter tests

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: oursky/flutter:latest

    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: flutter pub get
      - name: Run tests
        run: flutter test
